# 🚀 Streamlit Community Cloud 배포 가이드

## ✅ 준비 완료된 파일들

- ✅ `독립_대시보드_앱.py` - 메인 앱 (Secrets 사용하도록 수정됨)
- ✅ `requirements.txt` - Python 패키지 목록
- ✅ `packages.txt` - 시스템 패키지 목록
- ✅ `.streamlit/secrets.toml` - 로컬 개발용 DB 정보
- ✅ `.streamlit/config.toml` - Streamlit 설정
- ✅ `.gitignore` - 민감 정보 제외
- ✅ `README.md` - 프로젝트 설명

---

## 📋 배포 단계 (5분 소요)

### 1️⃣ GitHub 저장소 만들기

1. https://github.com 접속 후 로그인
2. 오른쪽 상단 `+` 버튼 → `New repository` 클릭
3. 설정:
   - Repository name: `neohelios-cruise-dashboard`
   - Description: `크루즈 객실 현황 대시보드`
   - ⚠️ **Private** 선택 (중요!)
4. `Create repository` 클릭

### 2️⃣ 코드 업로드하기

#### 방법 A: GitHub Desktop 사용 (추천, 쉬움)

1. [GitHub Desktop](https://desktop.github.com/) 다운로드 및 설치
2. GitHub Desktop 실행 → 로그인
3. `File` → `Add Local Repository`
4. `C:\Users\Lenovo\Desktop\NEOHELIOS_CRUISE` 선택
5. "Initialize Git repository" 클릭
6. 왼쪽 하단 Summary: `Initial commit` 입력
7. `Commit to main` 클릭
8. 상단 `Publish repository` 클릭
9. ⚠️ **Keep this code private** 체크 확인
10. `Publish Repository` 클릭

#### 방법 B: Git 명령어 사용

PowerShell에서:

```powershell
cd C:\Users\Lenovo\Desktop\NEOHELIOS_CRUISE

# Git 초기화
git init

# 모든 파일 추가
git add .

# 커밋
git commit -m "Initial commit"

# 브랜치 이름 변경
git branch -M main

# GitHub 저장소 연결 (YOUR_USERNAME을 본인 GitHub 아이디로 변경)
git remote add origin https://github.com/YOUR_USERNAME/neohelios-cruise-dashboard.git

# 업로드
git push -u origin main
```

### 3️⃣ Streamlit Community Cloud 배포

1. https://share.streamlit.io 접속
2. `Sign in with GitHub` 클릭 → GitHub 계정으로 로그인
3. 권한 요청 나오면 `Authorize streamlit` 클릭
4. `New app` 버튼 클릭
5. 설정:
   - **Repository:** `YOUR_USERNAME/neohelios-cruise-dashboard`
   - **Branch:** `main`
   - **Main file path:** `독립_대시보드_앱.py`
6. ⚠️ **Advanced settings** 클릭 (중요!)
7. **Secrets** 탭에서 아래 내용 **그대로** 붙여넣기:

```toml
[database]
server = "neohelios-prod.database.windows.net"
base_database = "neohelios_base"
cruise_database = "neohelios_cruise"
username = "panstar_viewer"
password = "Vostmxk0712!"
```

8. `Deploy!` 버튼 클릭
9. 2-3분 대기 ⏳

### 4️⃣ 완료! 🎉

- 앱이 자동으로 배포됩니다
- URL 형식: `https://neohelios-cruise-dashboard-xxxxx.streamlit.app`
- 이 URL을 팀원들과 공유하세요!
- 즐겨찾기에 추가하세요!

---

## ⚠️ 문제 해결

### 1. "Could not connect to database" 오류

**원인:** Azure SQL Server가 Streamlit Cloud IP를 차단함

**해결방법:**
1. Azure Portal 접속
2. SQL Server 선택: `neohelios-prod`
3. 왼쪽 메뉴 → `Networking` (또는 `Firewalls and virtual networks`)
4. 다음 중 하나 선택:
   - **방법 A (권장):** `Allow Azure services and resources to access this server` → `Yes`
   - **방법 B:** `Add a firewall rule` → IP range: `0.0.0.0` - `255.255.255.255` (모든 IP 허용, 보안 주의)
5. `Save` 클릭
6. Streamlit 앱에서 `Rerun` 클릭

### 2. "ModuleNotFoundError" 오류

**원인:** 패키지가 설치되지 않음

**해결방법:**
1. `requirements.txt` 파일이 GitHub에 올라갔는지 확인
2. Streamlit Cloud 대시보드에서 `Reboot app` 클릭

### 3. "pyodbc.Error: ('01000'..." 오류

**원인:** ODBC 드라이버 문제

**해결방법:**
1. `packages.txt` 파일이 GitHub에 올라갔는지 확인
2. Streamlit Cloud 대시보드에서 `Clear cache` → `Reboot app` 클릭

### 4. 앱이 느려요

**원인:** 무료 플랜은 리소스가 제한됨

**해결방법:**
- 정상입니다. 첫 로딩은 10-15초 소요될 수 있습니다
- 이후 조회는 빠릅니다
- 유료 플랜으로 업그레이드하면 더 빠릅니다

---

## 🔄 앱 업데이트 방법

코드를 수정한 후:

### GitHub Desktop 사용:

1. GitHub Desktop 실행
2. 변경된 파일 확인
3. Summary 입력: `버그 수정` 또는 `새 기능 추가`
4. `Commit to main` 클릭
5. `Push origin` 클릭
6. 자동으로 Streamlit 앱이 재배포됩니다 (1-2분 소요)

### Git 명령어 사용:

```powershell
git add .
git commit -m "업데이트 내용"
git push
```

---

## 📊 모니터링

Streamlit Community Cloud 대시보드에서:
- 앱 상태 확인
- 로그 보기 (오류 확인)
- 재시작 (Reboot app)
- 삭제 (Delete app)

URL: https://share.streamlit.io

---

## 💡 팁

1. **Private 저장소 사용**: DB 정보 보호를 위해 필수!
2. **Secrets 백업**: Streamlit Cloud의 Secrets 설정을 스크린샷으로 백업
3. **정기 백업**: GitHub에 코드가 자동으로 백업됩니다
4. **팀원 추가**: Private 저장소에 Collaborator로 추가 가능

---

## 🆘 도움이 필요하면

1. Streamlit 공식 문서: https://docs.streamlit.io
2. Streamlit 커뮤니티: https://discuss.streamlit.io
3. GitHub 이슈: 저장소에서 Issues 탭 사용

---

**축하합니다! 이제 전 세계 어디서나 대시보드를 사용할 수 있습니다! 🌍**

